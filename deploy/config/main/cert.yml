apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: witness-it.com
  namespace: default
spec:
  secretName: witness-it.com-production
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: witness-it.com
  dnsNames:
    - witness-it.com
